//! The ceiling on any single citizen's vote weight.

/// The ceiling on any single citizen's vote weight, so even the most-favoured
/// member cannot outweigh a handful of ordinary ones.
pub const MAX_VOTE_WEIGHT: u64 = 16;
