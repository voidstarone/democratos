[package]
name = "adapter-cli"
edition.workspace = true
version.workspace = true
license.workspace = true

# A driving adapter: a command-line interface over the SAME application services
# the web adapter uses. This is the "API <-> CLI" swap made concrete — both call
# `app::Services`; neither owns business logic.
[dependencies]
domain = { workspace = true }
app = { workspace = true }
clap = { workspace = true }
anyhow = { workspace = true }
